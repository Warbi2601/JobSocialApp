<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:app="clr-namespace:JobSocialApp"
             xmlns:input="clr-namespace:Plugin.InputKit.Shared.Controls;assembly=Plugin.InputKit"
             x:Class="JobSocialApp.Views.SettingsView">
    
    <ContentPage.Resources>
        <ResourceDictionary MergedWith="app:StylingResourceDictionary" />
    </ContentPage.Resources>

    <ScrollView VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Orientation="Horizontal" Margin="20, 10">
        <StackLayout Orientation="Vertical">
            <Label StyleClass="MediumTitleLabel" Text="{Binding LanguageOptionsLbl}"/>
            <Label Text="{Binding ChooseLanguageLbl}"/>
            <input:RadioButtonGroupView SelectedItem="{Binding SelectedLanguage}">
                <input:RadioButton Value="en" Clicked="RadioLanguageButton_Clicked" Text="{Binding EnglishRadio}" />
                <input:RadioButton Value="pl" Clicked="RadioLanguageButton_Clicked" Text="{Binding PolishRadio}" />
            </input:RadioButtonGroupView>
        </StackLayout>
    </ScrollView>

</ContentPage>